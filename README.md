# CHIC

C++ project with Python bindings (pybind11) and tests for both C++ and Python components.

## Structure

- `cpp/`: C++ source and headers
- `python/`: Python package and bindings
- `tests/`: Tests for C++ and Python

## Build instructions

See each subdirectory for build/readme stubs.
