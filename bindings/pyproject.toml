[build-system]
requires = ["setuptools>=64", "pybind11>=2.6"]
build-backend = "setuptools.build_meta"

[project]
name = "chic"
version = "1.1.0"
description = "Python bindings for CHIC"
requires-python = ">=3.8"
dependencies = ["numpy", "pybind11>=2.6"]

[tool.setuptools]
zip-safe = false
